archivesBaseName = "stn-core"

dependencies {
    // Core has no mod dependencies - it's the base
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
    repositories {
        mavenLocal()
    }
}
