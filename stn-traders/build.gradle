archivesBaseName = "stn-traders"

dependencies {
    // Depends on stn-core
    implementation project(':stn-core')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
    repositories {
        mavenLocal()
    }
}
